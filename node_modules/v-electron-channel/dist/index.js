'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var Handler;
if (process.type === 'browser') {
    Handler = require('./browser');
}
else {
    Handler = require('./renderer');
}
/**
* 注册一个通道
* @param channel
*/
function register(channel) {
    return Handler.register(channel);
}
exports.register = register;
/**
* 删除一个通道
* @param channel
*/
function unregister(channel) {
    return Handler.unregister(channel);
}
exports.unregister = unregister;
/**
* 查询通道对应的 webContext id
* @param channel
*/
function query(channel) {
    return Handler.query(channel);
}
exports.query = query;
